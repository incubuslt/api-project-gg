API task for AtlantisGames

app.js

## Requirements
* node v15

## npm packages:
* express
* swagger-ui-expres
* swagger-jsdo
* nanoid

command line to install:
npm install express swagger-ui-express swagger-jsdoc nanoid